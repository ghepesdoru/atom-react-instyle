'use babel';

import AtomReactInstyleView from '../lib/atom-react-instyle-view';

describe('AtomReactInstyleView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
